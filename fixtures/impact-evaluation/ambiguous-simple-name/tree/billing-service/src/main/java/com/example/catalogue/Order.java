package com.example.catalogue;

/** A different Order entirely. Same simple name, unrelated type. */
public class Order {
    private int sequence;

    public int getSequence() {
        return sequence;
    }
}
