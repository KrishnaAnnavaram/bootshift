{
  "id": "mockbean-deprecated",
  "description": "@MockBean was superseded by the Spring Framework bean-override annotations. Only the test that declares it is affected; the production class it mocks is not.",
  "tuning": false,
  "fact": {
    "type": "API_REMOVED",
    "subject": "org.springframework.boot.test.mock.mockito.MockBean",
    "from": "MockBean",
    "to": "MockitoBean"
  },
  "true_affected_paths": [
    "employee-service/src/test/java/com/example/service/EmployeeServiceTest.java"
  ],
  "ground_truth_source": "Hand-declared by inspection of the tree below. The harness runs ImpactStage's matcher over the tree and compares its output to this list; the fixture never states what the matcher should predict."
}
